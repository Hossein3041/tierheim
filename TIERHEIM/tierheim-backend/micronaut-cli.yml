applicationType: default
defaultPackage: solutions.sideklick
testFramework: junit
sourceLanguage: java
buildTool: maven
features:
  [
    app-name,
    aws-lambda,
    aws-lambda-custom-runtime,
    aws-lambda-events-serde,
    aws-lambda-handler-default,
    graalvm,
    http-client,
    http-client-test,
    java,
    junit,
    logback,
    maven,
    maven-enforcer-plugin,
    micronaut-aot,
    properties,
    readme,
    serialization-jackson,
    shade,
    x86,
  ]
